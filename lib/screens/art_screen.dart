import 'package:flutter/material.dart';
import 'exercise_screen.dart';

class ArtScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ExerciseScreen(title: 'Cores');
  }
}
